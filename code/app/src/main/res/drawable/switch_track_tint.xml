<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ON (checked) -->
    <item android:color="@color/primaryColor" android:state_checked="true" />
    <!-- OFF (unchecked) -->
    <item android:color="@color/tertiaryColor" />
</selector>
